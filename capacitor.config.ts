import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-aps-1963061',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
